{
  "scripts": {
    "start": "concurrently \"npm run api\" \"npm run frontend\"",
    "frontend": "cd frontend && npm start",
    "api": "cd api && npm run dev"
  },
  "dependencies": {
    "concurrently": "^9.1.0"
  }
}
